[project]
name = "mass"
version = "5.0.1"
description = "Metadata Artifact Search Service - A service for searching metadata artifacts and filtering results."
dependencies = [
    "typer>=0.15.2",
    "ghga-service-commons[api]>=4.0.0",
    "ghga-event-schemas>=5.0.0, <6",
    "hexkit[mongodb,akafka]@git+https://github.com/ghga-de/hexkit@fix/otel_distro_issues",
]

[project.urls]
Repository = "https://github.com/ghga-de/mass"

[project.scripts]
mass = "mass.__main__:cli"
